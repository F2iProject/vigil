# Vigil — AI-powered PR review
# Copy to .env and fill in the keys you need.

# GitHub personal access token (needs repo scope for PR access)
GITHUB_TOKEN=

# Model API keys — only need the one matching your --model choice
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
